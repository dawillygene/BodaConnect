{
  "$schema": "https://www.schemastore.org/package.json",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "php artisan serve",
    "dev": "echo 'Frontend now lives in ./frontend. Run npm install && npm run dev inside frontend/.'",
    "build": "echo 'Build the React app from ./frontend with npm run build.'"
  }
}
