{
  "$schema": "https://www.schemastore.org/package.json",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "php artisan serve",
    "dev": "echo 'Vite/Tailwind native build disabled on this environment (Bus error in @tailwindcss/oxide). Using Blade + Tailwind CDN fallback.'",
    "build": "echo 'Build skipped: using Tailwind CDN fallback in Blade layouts.'"
  }
}
